using UnityEngine;
using UnityEngine.Events;

namespace Plugins.CameraTouchController
{
    public class TouchEventsHandler : MonoBehaviour
    {
        //public UnityEvent

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
